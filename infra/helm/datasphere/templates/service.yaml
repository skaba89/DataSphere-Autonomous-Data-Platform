apiVersion: v1
kind: Service
metadata:
  name: {{ include "datasphere.fullname" . }}
  labels:
    {{- include "datasphere.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datasphere.selectorLabels" . | nindent 4 }}
