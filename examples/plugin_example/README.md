# DataSphere Plugin Example

This example shows how to create a DataSphere generator plugin.

## Installation
pip install -e .

## Usage
Once installed, the plugin appears automatically in GET /plugins
